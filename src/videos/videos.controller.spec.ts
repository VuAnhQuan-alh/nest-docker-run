import { VideosController } from './videos.controller';
import { VideosService } from './videos.service';

describe('VideosController', () => {
  let videosCtrl: VideosController;
  let videosServ: VideosService;

  beforeEach(() => {
    // videosServ = new VideosService();
    // videosCtrl = new VideosController(videosServ);
  });
});
